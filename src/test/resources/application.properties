vehicle.line.lookup.enabled=false
vehicle.operator.lookup.enabled=false
vehicle.nsr.lookup.enabled=false
vehicle.serviceJourney.lookup.enabled=false
vehicle.datedserviceJourney.lookup.enabled=false

vehicle.journeyplanner.url=http://localhost:0/graphql
vehicle.journeyplanner.EtClientName=test

vehicle.updates.max.validity.minutes=1440

# Pub/Sub is not exercised by any test - these values only need to satisfy
# required @Value placeholders on the (disabled) subscriber beans; no network
# call is made using them.
entur.vehicle-positions.gcp.topic.project.name=test-project
entur.vehicle-positions.gcp.subscription.project.name=test-project
entur.vehicle-positions.gcp.topic.name.vm=test.vm
entur.vehicle-positions.gcp.topic.name.et=test.et
entur.vehicle-positions.gcp.topic.name.sx=test.sx
entur.vehicle-positions.gcp.subscription.name.vm=test.vm.sub
entur.vehicle-positions.gcp.subscription.name.et=test.et.sub
entur.vehicle-positions.gcp.subscription.name.sx=test.sx.sub
entur.vehicle-positions.gcp.labels={app:'vehicle-positions',team:'ror'}

# All three Pub/Sub subscribers explicitly disabled for tests, regardless of
# each subscriber's own default.
entur.vehicle-positions.vm.enabled=false
entur.vehicle-positions.et.enabled=false
entur.vehicle-positions.sx.enabled=false
